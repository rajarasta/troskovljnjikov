[project]
name = "company-agent-ui"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai",
    "streamlit",
    "pandas",
    "openpyxl",
    "openai",
    "httpx",
]

[tool.uv]
package = true
